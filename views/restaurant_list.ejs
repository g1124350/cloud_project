<html>
    <head>
        <title>Restaurant</title>
    </head>
    <body>
        <H2>Showing <%= photos.length %> document(s)</H2>
        <ol>
            <% for (i in photos) { %>
                <li><a href=/display?_id=<%= reataurant[i]._id%>><%=photos[i].title%></a></li>
            <% } %>
        </ol>
    </body>
</html>
